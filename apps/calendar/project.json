{
  "name": "calendar",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "apps/calendar/src",
  "targets": {
    "config-local-hostile": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "apps/calendar",
        "commands": [
          "sudo pnpm exec hostile set localhost calendar.vezham.local && sudo pnpm exec hostile set localhost calendar.mock.vezham.local"
        ],
        "parallel": false
      }
    }
  },
  "tags": ["category:app"]
}
