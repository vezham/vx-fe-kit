{
  "name": "contact2",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "apps/contact2/src",
  "targets": {
    "config-local-hostile": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "apps/contact2",
        "commands": [
          "sudo pnpm exec hostile set localhost contact2.vezham.local && sudo pnpm exec hostile set localhost contact2.mock.vezham.local"
        ],
        "parallel": false
      }
    }
  },
  "tags": ["category:app"]
}
