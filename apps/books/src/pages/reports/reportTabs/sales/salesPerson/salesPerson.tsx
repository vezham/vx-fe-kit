import { Button } from '@vx-oss/react'

const SalesPerson = () => {
  return (
    <div className="flex h-screen flex-col items-center justify-center">
      <Button color="default" variant="flat">
        Sales Person Content not found
      </Button>
    </div>
  )
}

export default SalesPerson
