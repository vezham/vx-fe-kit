{
  "name": "reminder",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "apps/reminder/src",
  "targets": {
    "config-local-hostile": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "apps/reminder",
        "commands": [
          "sudo pnpm exec hostile set localhost reminder.vezham.local && sudo pnpm exec hostile set localhost reminder.mock.vezham.local"
        ],
        "parallel": false
      }
    }
  },
  "tags": ["category:app"]
}
