import { Button } from '@vezham/react/v2'

const itemSold = () => {
  return (
    <div className="flex h-screen flex-col items-center justify-center gap-2">
      <Button color="default" variant="flat">
        Itemsold Content not found
      </Button>
    </div>
  )
}

export default itemSold
