{
  "name": "notes2",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "apps/notes2/src",
  "targets": {
    "config-local-hostile": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "apps/notes2",
        "commands": [
          "sudo pnpm exec hostile set localhost notes2.vezham.local && sudo pnpm exec hostile set localhost notes2.mock.vezham.local"
        ],
        "parallel": false
      }
    }
  },
  "tags": ["category:app"]
}
