{
  "name": "ts-basic",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "apps/ts-basic/src",
  "targets": {
    "config-local-hostile": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "apps/ts-basic",
        "commands": [
          "sudo pnpm exec hostile set localhost ts-basic.vezham.local && sudo pnpm exec hostile set localhost ts-basic.mock.vezham.local"
        ],
        "parallel": false
      }
    }
  },
  "tags": ["category:app"]
}
