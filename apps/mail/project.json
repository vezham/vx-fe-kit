{
  "name": "mail",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "apps/mail/src",
  "targets": {
    "config-local-hostile": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "apps/mail",
        "commands": [
          "sudo pnpm exec hostile set localhost mail.vezham.local && sudo pnpm exec hostile set localhost mail.mock.vezham.local"
        ],
        "parallel": false
      }
    }
  },
  "tags": ["category:app"]
}
