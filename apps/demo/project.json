{
  "name": "demo",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "apps/demo/src",
  "targets": {
    "build": {
      "dependsOn": ["metadata:generate", "deploy:generate", "^build"],
      "outputs": ["{projectRoot}/.output"]
    },
    "dev": {
      "dependsOn": ["metadata:watch"]
    },
    "hosts:setup": {},
    "deploy:generate": {},
    "metadata:generate": {},
    "metadata:watch": {},
    "routes:tanstack": {}
  },
  "tags": ["category:apps"]
}
