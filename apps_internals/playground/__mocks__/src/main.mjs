// @vx/NOTE: GENERATE BY vx/scripts/apps/config-mocks.ts DO NOT EDIT THIS FILE DIRECTLY

import { app } from './app.mjs'
const hostname = process.env.CI
  ? 'localhost'
  : process.env.V_MOCK_HOST_NAME || 'localhost'
const port = process.env.V_MOCK_PORT || 3030
const name = process.env.V_MOCK_APP_NAME || 'vx-app-mock'

app.listen(port, hostname, () =>
  console.log(`[INFO] ${name} | server is running | http://${hostname}:${port}`)
)
// app.listen(port, () =>
//   console.log(`[INFO] ${name} | server is running | http://${hostname}:${port}`)
// )
